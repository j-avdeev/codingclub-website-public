---
interface Props {
  id?: string;
}
const { id } = Astro.props;
---

<h1 id={id} class="mb-5 mt-14 px-4 text-center text-[clamp(2rem,5vw,2.85rem)] font-extrabold tracking-normal text-slate-950 md:mt-16">
  <slot />
</h1>
